services:
  caddy:
    build:
      dockerfile: Dockerfile.caddy
    restart: unless-stopped
    networks:
      - net
    volumes:
      - caddy-data:/data
      - caddy-config:/config
    environment:
      CADDY_ADMIN: 0.0.0.0:2019

volumes:
  caddy-data:
  caddy-config:

